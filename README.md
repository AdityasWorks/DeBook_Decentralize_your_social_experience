# DeBook


To run the project just clone it and then type these in the terminal

```shell
npm install
```
to install all the libraries used in this project

```shell
npx parcel index.html
```
for bundling the packages

